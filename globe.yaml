# yaml-language-server: $schema=https://globe.dev/globe.schema.json

entrypoint: lib/main.server.dart

build:
  preset:
    type: jaspr_static
    version: 0.22.0
  build_runner:
    automatic_detection: false
  melos:
    automatic_detection: false